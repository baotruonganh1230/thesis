spring:
  datasource:
    url: jdbc:mysql://localhost:3306/hrms_prod
    username: hrms_prod
    password: Abc1230g
  jpa:
    hibernate:
      ddl-auto: validate
    database-platform: org.hibernate.dialect.MySQL5InnoDBDialect
    database: mysql
    show-sql: false
  sql:
    init:
      platform: mysql